import ContactClient from "./ContactClient";

/**
 * Contact Page (Server Component)
 * Static layout parts can be moved here in the future to reduce client JS.
 */
export default function ContactPage() {
  return <ContactClient />;
}
